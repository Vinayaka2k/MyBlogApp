name: main

on: 
  push:
    branches:
      - main
      
jobs:
  cicd_pipeline:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with: 
          node_version: '16'
      - name: npm install
        run: npm install
